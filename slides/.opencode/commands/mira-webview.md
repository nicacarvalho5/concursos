---
description: "Coloca um site vivo dentro de um slide do Mira (novo ou existente), ocupando o slide inteiro (edge to edge) via iframe full-bleed. Resolve os dois problemas de embutir um site num deck: o iframe rouba o foco do teclado (uma guarda de interação deixa o site só visível até o apresentador clicar, e Esc devolve o controle ao deck) e sites que bloqueiam incorporação por X-Frame-Options ou CSP frame-ancestors (fallback com link, ou baixar o site para assets/webview/ e rodar offline por file protocol). Herda a Regra Zero do mira-animator. Use SEMPRE que o usuário disser /mira-webview, webview, embutir site, colocar um site no slide, site dentro do slide, iframe no slide, incorporar página, mostrar um site na apresentação, abrir um site no deck, ou pedir um slide que seja uma página web ao vivo."
agent: build
---
# Comando `mira-webview` do Mira
Ative este comando para carregar o skill `mira-webview` e siga as instruções do SKILL.md.
/mira-webview
